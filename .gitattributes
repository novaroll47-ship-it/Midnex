# Иначе shell-скрипты, уехавшие с Windows с CRLF, не запустятся на сервере.
# Сервер — Linux, поэтому в репозитории всё хранится с LF.
* text=auto eol=lf
*.bat text eol=crlf
*.docx binary
*.png binary
*.sh text eol=lf
